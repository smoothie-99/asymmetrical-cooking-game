namespace Interactions
{
    public enum InteractionType
    {
        Primary,   // E key (Interact/Pickup)
        Secondary, // F key (Special Action/Take out)
        UseItem    // Left Click (Use held item on station)
    }
}
